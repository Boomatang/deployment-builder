general:
  name: advanced-deployment
  version: 2.1.0
  environment: staging
  prefix: advanced-project
  kubeconfig_path: kubeconfigs

# Cluster configuration
clusters:
  metrics:
    enable: true
  primary:
    count: 3
  secondary:
    count: 2
  standard:
    count: 5

